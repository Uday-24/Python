# Definition :- Calculate the area of a triagle
# Area = (1/2) * height * base

height = float(input("Enter height for triangle : "))
base = float(input("Enter base for triangle : "))

area = (1/2) * height * base

print("Area of a Triangle = ", area)